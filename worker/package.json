{
  "name": "tensorfeed-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240529.0",
    "vitest": "^1.6.0",
    "wrangler": "^3.60.0"
  }
}
