name: Weekly AI infrastructure review

# Opens a GitHub issue every Monday at 14:00 UTC (07:00 PT) summarizing
# whether data/ai-infrastructure-projects.json needs new entries or
# status updates. Operator triages the issue, edits the file if
# something materially shifted, otherwise closes.
# See scripts/weekly-ai-infrastructure-check.mjs for the report body logic.

on:
  schedule:
    - cron: '0 14 * * 1'   # 14:00 UTC every Monday
  workflow_dispatch:        # allow manual runs from the Actions tab

permissions:
  issues: write
  contents: read

jobs:
  open-review-issue:
    runs-on: ubuntu-latest
    if: ${{ github.repository == 'RipperMercs/tensorfeed' }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Generate report body
        id: report
        run: |
          node scripts/weekly-ai-infrastructure-check.mjs > issue-body.md
          {
            echo "title=Weekly AI infrastructure review: $(date -u +%Y-%m-%d)"
          } >> "$GITHUB_OUTPUT"

      - name: Open issue
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh issue create \
            --title "${{ steps.report.outputs.title }}" \
            --body-file issue-body.md \
            --label "ai-infrastructure,maintenance"
